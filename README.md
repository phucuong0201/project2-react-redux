Clone this project, then run the following steps:

  - cd to project folder
  - ```npm install``` (if error due to permission issues, run: ```sudo npm install```)
  - ```npm start```
  - go to browser and type to address bar: ```localhost:3000```

